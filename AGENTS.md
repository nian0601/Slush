# AGENTS.md

This is a Codex adapter for the guidance in:

- `C:\Users\Ander\.claude\CLAUDE.md`
- `CLAUDE.md`

Before working, read both files in that order and follow them as the source of truth. The
repo-local `CLAUDE.md` layers project-specific guidance on top of the global file and wins
when the two conflict.

Treat references to Claude Code as references to the active coding agent unless the
instruction is explicitly about Claude-only discovery, settings, skills, or runtime
behavior.
